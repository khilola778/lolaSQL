DELETE: Removes specific rows from a table using WHERE. Can be rolled back.
TRUNCATE: Removes all rows quickly without WHERE. Cannot be rolled back in most DBMS.
DROP: Deletes the entire table structure and data. Cannot be rolled back.
